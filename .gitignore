# OS junk
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Env files (these can carry real secrets; ignore by default)
.env
.env.local
.env.*.local

# Local notes and scratch
TODO.local.md
NOTES.local.md
.scratch/

# Claude session artifacts: keep the project-rule file tracked so Claude can
# read it on every session, but ignore everything else (lock files, scheduled
# task state, image caches, worktrees).
.claude/*
!.claude/CLAUDE.md
